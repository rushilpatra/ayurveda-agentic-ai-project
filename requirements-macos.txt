# Pinned dependencies for Apple silicon (arm64) macOS.
#
# Verified on: macOS 15.6.1, Apple M4, 16 GB unified memory, Python 3.11.13.
# Install into a project-local venv via `make setup` - this never touches an
# existing environment.
#
# Deliberately absent, and why:
#   bitsandbytes  - CUDA-only, does not build on Apple silicon
#   vllm          - CUDA-only
#   faiss-cpu     - unreliable wheels on arm64; hnswlib is used instead
#   accelerate    - not needed; MLX handles device placement
#
# No Homebrew packages are required. No Docker.

# --- core numerics -----------------------------------------------------------
# numpy pinned <2 for binary compatibility with torch 2.2.x wheels.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0
pyarrow==16.1.0

# --- local LLM inference (Apple MLX) -----------------------------------------
# mlx-lm pulls mlx; both ship native arm64 wheels with Metal support.
mlx==0.29.3
mlx-lm==0.28.4

# --- retrieval ---------------------------------------------------------------
rank-bm25==0.2.2
hnswlib==0.8.0
sentence-transformers==2.7.0
# torch is used only by sentence-transformers, on MPS or CPU.
torch==2.2.2

# --- knowledge graph ---------------------------------------------------------
networkx==3.3

# --- data access -------------------------------------------------------------
huggingface-hub==0.27.0
datasets==2.19.2

# --- reporting ---------------------------------------------------------------
matplotlib==3.10.7
tqdm==4.66.4
PyYAML==6.0.1

# --- testing -----------------------------------------------------------------
pytest==8.2.2
